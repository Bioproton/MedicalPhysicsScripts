Files used to automate FLUKA simulations

In general, the automation has two main jobs:
    1) Automatically run sequential simulations (i.e. overnight and so on)
    2) After finishing one simulation:
        - Remove uneccasary files
        - Sort outpt files into folders